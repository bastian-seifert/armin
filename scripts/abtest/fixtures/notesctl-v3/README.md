# notesctl

A minimal note-taking command line tool.

    python3 -m notesctl add "buy milk"
    python3 -m notesctl list

Status: skeleton — the CLI parses arguments but no storage backend is
implemented yet. Standard library only; no third-party dependencies.
